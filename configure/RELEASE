# RELEASE
# Defines location of external products
include $(TOP)/RELEASE_SITE

<<<<<<< HEAD
# ===============================================================
# Define the version(s) of any other needed modules 
# ===============================================================
DIAG_TIMER_MODULE_VERSION       = R1.9.1
IPAC_MODULE_VERSION             = R2.8-1.2.2
EVENT2_MODULE_VERSION           = R4.3.0
TIMESYNC_MODULE_VERSION         = R1.1.11
BLDCLIENT_MODULE_VERSION        = R2.18.0
=======
# =============================================================
# Define location of module tree with respect to
# EPICS_SITE_TOP
# =============================================================
EPICS_MODULES=$(MODULES_SITE_TOP)

# =============================================================
# Define the version of modules needed by
# IOC apps or other Support apps
# =============================================================
IPAC_MODULE_VERSION=R2.11-1.5.0
>>>>>>> ip330-R2-3-0

# ============================================================
# External Support module path definitions
#
# If any of these macros expand to a path which
# contains an "include" directory, that directory will be
# included in the compiler include path.
#
# If any of these macros expand to a path which
# contains a "lib/<arch>" directory, that directory will be
# included in the compiler link path for that architecture.
#
# If your build fails, look for these paths in your build output
# ============================================================
DIAG_TIMER      = $(EPICS_MODULES)/diagTimer/$(DIAG_TIMER_MODULE_VERSION)
IPAC            = $(EPICS_MODULES)/ipac/$(IPAC_MODULE_VERSION)
EVENT2          = $(EPICS_MODULES)/event2/$(EVENT2_MODULE_VERSION)
TIMESYNC        = $(EPICS_MODULES)/timesync/$(TIMESYNC_MODULE_VERSION)
BLDCLIENT       = $(EPICS_MODULES)/bldClient/$(BLDCLIENT_MODULE_VERSION)

# =====================================================================
# Set EPICS_BASE last so it appears last in the DB, DBD, INCLUDE, 
# and LIB search paths
# ====================================================================
EPICS_BASE=$(BASE_SITE_TOP)/$(BASE_MODULE_VERSION)

